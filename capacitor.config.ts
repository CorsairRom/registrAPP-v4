import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'registrAPP-v1',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
